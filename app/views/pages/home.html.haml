-provide(:title, 'Unlist.it')

//Code for the quick-search bar
.row
  #searcharea.col-xs-8
    =render partial: 'shared/quicksearch'
    %br

    %p.status-text.text-muted Have something to sell? Search & see if any unlisters are looking for it.
    //%p.status-text.text-muted="There are currently #{User.count} Unlisters looking for #{Unlisting.count} things."


  -unless signed_in?
    #signup.col-xs-4
      =link_to signup_path do
        %h2
          Wishlist Classifieds
          %br
          Sign Up & Make Your Wishlist
          %p
            (yep, it's free.)


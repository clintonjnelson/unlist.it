require 'spec_helper'

describe MessagesManager do
  let!(:settings) { Fabricate(:setting) }

  context "for a new Unlisting message"
    context "by a User"
      context "who is allowed"
      context "who is unconfirmed"
      context "who is suspended"
    context "by a Safeguest"
      context "who is allowed"
      context "who is allowed"
      context "who is allowed"
  context "for a new Unlisting message"
  context "for a new Unlisting message"
  context "for a new Unlisting message"
  context "for a new Unlisting message"
end

